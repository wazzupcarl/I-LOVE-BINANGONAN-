document.addEventListener('DOMContentLoaded', function() {
            
    // ===================================
    // MOJUL AND PACTIONS 🤞 
    // ===================================
    
    const mainModal = document.getElementById("myModal");
    const modalTitle = document.getElementById("modalTitle");
    const modalDesc = document.getElementById("modalDesc");
    
    const closeModalIcon = document.querySelector(".close-icon");
    const okButton = document.querySelector(".modal-ok-btn");




    function openInfoModal(title, description) {
        modalTitle.innerText = title;
        modalDesc.innerText = description;
        mainModal.style.display = "flex";
    }




    const modalTriggers = document.querySelectorAll("#readMoreBtn, .country__card");
    
    modalTriggers.forEach(btnOrCard => {
        btnOrCard.addEventListener("click", (evt) => {
            evt.preventDefault();
            
            const titleTxt = btnOrCard.getAttribute("data-title");
            const descTxt = btnOrCard.getAttribute("data-desc");




            if(titleTxt && descTxt) {
                openInfoModal(titleTxt, descTxt);
            }
        });
    });




    function closeTheModal() {
        mainModal.style.display = "none"; 
    }
    
    closeModalIcon.addEventListener("click", closeTheModal);
    okButton.addEventListener("click", closeTheModal);




    window.addEventListener("click", (e) => { 
        if (e.target === mainModal) closeTheModal();
    });




    // FOR CONTROL #0 🤞
    window.addEventListener("scroll", () => {
        const mainNav = document.querySelector("nav");
        if(mainNav) mainNav.classList.toggle("nav--scrolled", window.scrollY > 20);
    });




    const menuButton = document.querySelector(".menu-btn");
    const navContainer = document.querySelector(".nav__links");
    const allNavLinks = document.querySelectorAll(".nav__links .link");




    if(menuButton && navContainer){
        menuButton.addEventListener("click", () => {
            navContainer.classList.toggle("show");
            menuButton.classList.toggle("open");
            
            if (menuButton.classList.contains("open")) {
                menuButton.innerHTML = `<i class="ri-close-line"></i>`;
            } else {
                menuButton.innerHTML = `<i class="ri-menu-line"></i>`;
            }
        });
    }




    allNavLinks.forEach(link => {
        link.addEventListener("click", () => {
            const targetSectionId = link.dataset.scroll;
            const sectionEl = document.getElementById(targetSectionId);




            if(navContainer) navContainer.classList.remove("show");
            if(menuButton) {
                menuButton.classList.remove("open");
                menuButton.innerHTML = `<i class="ri-menu-line"></i>`;
            }
            
            allNavLinks.forEach(l => l.classList.remove("active"));
            link.classList.add("active");




            if(sectionEl) sectionEl.scrollIntoView({ behavior: "smooth" });
        });
    });
});